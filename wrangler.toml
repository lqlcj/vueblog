# ===============================================
# 1. 必填信息
# ===============================================
name = "vueblog"      # 👈 必填：您的 Worker 名称 (唯一，小写，使用 - 连接)
main = "./worker/index.js"       # 👈 必填：Worker 的入口文件（我们稍后创建）
compatibility_date = "2024-11-28" # 必填：使用最新的日期确保兼容性

# ===============================================
# 2. Worker Sites 静态文件配置 (关键)
# ===============================================
[site]
bucket = "./dist"  # 👈 必须指向 Vue/Vite 打包后的文件夹！